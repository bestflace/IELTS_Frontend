import { Card } from '@/components/common/Card';
export function GradingStatusTimeline({children}:{children?:React.ReactNode}){return <Card className='p-4'><p className='font-medium'>GradingStatusTimeline</p>{children}</Card>}
