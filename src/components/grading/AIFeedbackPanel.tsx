import { Card } from '@/components/common/Card';
export function AIFeedbackPanel({children}:{children?:React.ReactNode}){return <Card className='p-4'><p className='font-medium'>AIFeedbackPanel</p>{children}</Card>}
