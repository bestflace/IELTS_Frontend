import { Card } from '@/components/common/Card';
export function TagFormModal({children}:{children?:React.ReactNode}){return <Card className='p-4'><p className='font-medium'>TagFormModal</p>{children}</Card>}
