import { Card } from '@/components/common/Card';
export function TagBadge({children}:{children?:React.ReactNode}){return <Card className='p-4'><p className='font-medium'>TagBadge</p>{children}</Card>}
